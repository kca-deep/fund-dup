// Prisma client placeholder for UI development
// TODO: Configure Prisma 7 with prisma.config.ts when setting up database

export const prisma = {
  document: {
    findMany: async () => [],
    findUnique: async () => null,
    create: async () => ({ id: "temp" }),
    update: async () => ({}),
    delete: async () => ({}),
  },
  checkResult: {
    findMany: async () => [],
    findUnique: async () => null,
    create: async () => ({ id: "temp" }),
    update: async () => ({}),
    delete: async () => ({}),
  },
};

export default prisma;
